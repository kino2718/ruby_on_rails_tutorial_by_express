<!doctype html>
<html lang="ja">

<head>
    <title>
        <%= fullTitle(title) %>
    </title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta charset="utf-8">
    <link rel="stylesheet" href="/stylesheets/application.css?v=1.0">
</head>

<body>
    <header class="navbar fixed-top bg-dark">
        <div class="container">
            <a class="text-decoration-none" href="/" id="logo">sample app</a>
            <nav>
                <ul class="nav">
                    <li class="nav-item"><a class="nav-link text-light" href="/">Home</a></li>
                    <li class="nav-item"><a class="nav-link text-light" href="/help">Help</a></li>
                    <% if (hasLoggedIn) { %>
                        <li class="nav-item"><a class="nav-link text-light" href="#">Users</a></li>
                        <li class="nav-item dropdown">
                            <a href="#" id="account" class="nav-link text-light dropdown-toggle">
                                Account <b class="caret"></b>
                            </a>
                            <ul id="dropdown-menu" class="dropdown-menu">
                                <li><a href="/users/<%= currentUserId %>">Prifile</li>
                                <li><a href="#">Settings</li>
                                <li class="divider"></li>
                                <li>
                                     <a data-method="delete" href="/logout">Log out</a>
                                </li>
                            </ul>
                        </li>
                    <% } else { %>
                        <li class="nav-item"><a class="nav-link text-light" href="/login">Log in</a></li>
                    <% } %>
                </ul>
            </nav>
        </div>
    </header>
    <div class="container">
        <% Object.keys(flash).forEach(type => { %>
            <% flash[type].forEach(msg => { %>
                <div class="alert alert-<%= type %>"><%= msg %></div>
            <% }) %>
        <% }) %>
