<% if (object.errors) { %>
    <div id="error_explanation">
        <div class="alert alert-danger">
            <% const count = object.errors.fullMessages.length %>
            The form contains <%= count %> <%= count === 1 ? 'error' : 'errors' %>.
        </div>
        <ul>
            <% object.errors.fullMessages.forEach( msg => { %>
                <li><%= msg %></li>
            <% }) %>
        </ul>
    </div>
<% } %>
